# oGRecorder 主备参数同步

## 特性描述<a name="section16406154310471"></a>

本特性在主机修改配置文件后，无需手动在备机修改即可同步给备机。支持同步gr_inst.ini配置文件，并在同步后进行立即生效的参数加载。系统可以10min为周期定时进入同步流程，也可通过grcmd setcfg进入同步流程。

## 特性约束<a name="section06531946143616"></a>

- 以下参数列为保留参数：LOG_HOME、LOG_FILE_COUNT、INST_ID、LISTEN_ADDR，不进行同步。
- 本特性支持的参数同步必须依赖gr_inst.ini文件及worm存储。
- 加载配置文件时，遵守参数类型的加载规则，重启生效类型的参数必须重启节点才能生效。
- grcmd setcfg支持memory（仅内存）、pfile（仅文件）和both（内存+文件）三种设置模式，参数同步功能同样适配这三种模式：
  - 若通过memory模式设置参数，同步时备节点同样仅内存生效，文件不生效；
  - 若通过pfile模式设置参数，同步时备节点同样仅文件生效，内存不生效；
  - 若通过both模式设置参数，同步时备节点的参数值在文件与内存中都生效。
  - 所有参数同步均基于对应模式加载参数。例如：主节点通过grcmd setcfg分别设置日志级别为内存生效的255和文件生效的254，备节点会同步此配置，保持内存生效255、文件生效254的一致状态。
- 本特性仅支持立即生效类型的动态参数同步加载，对于重启生效类型的同步参数，只能通过同步流程将参数同步到备机的配置文件中，无法加载。比如：直接修改配置文件中某个重启生效类型的非保留参数值，使主备配置文件中该参数的值不一致后，重启整个集群，主备配置文件中该参数的值会被同步一致为主节点的值；直接在备机的配置文件里修改重启类型类型的非保留参数后，立刻重启备机，修改后的值会被共享文件中的值覆盖，但保留参数会生效。
- 同步流程中如果发生failover或switchover进行换主时，大部分情况都能同步最新参数，部分极端场景下需要用户在新主节点重新配置最新参数，比如在主机维护共享配置文件期间主机异常退出，会导致共享配置文件的内容无法被更新。

## 基本原理<a name="section8406461564716"></a>

在worm共享存储中维护共享配置文件gr_memory_sync.ini以及gr_pfile_sync.ini，由主机写入，备机不能修改。主机在写完共享配置文件后广播同步消息给备机，备机收到同步消息后读取共享配置文件的内容，更新本地配置文件并加载。集群发生主备切换后，新主先用共享配置文件中的内容更新本地配置文件，然后再作为主机发起后续同步流程。

## 使用指导<a name="section8445461564354"></a>

系统以10min的周期定时同步，用户也可通过以下操作触发同步：

- 使用cm_ctl start启动集群。
- 在主机使用grcmd setcfg 命令修改配置参数。
- 进行switchover、failover触发换主。此时，新主节点用共享存储的配置文件覆盖本机配置文件，然后备节点用共享存储的配置文件覆盖本机配置文件。
- 备机加入集群。备节点用共享存储的配置文件覆盖本机配置文件。

其中，进行switchover、failover触发换主时，新主节点和备节点均用共享存储里的配置文件覆盖本机配置文件；其他操作触发参数同步时，主节点把本机的配置文件写进共享存储，然后备节点用共享存储里的配置文件覆盖本机配置文件。

> 通过grcmd setcfg pfile/memory设置同步参数，会导致内存与文件不一致，若触发定时同步，文件内的立即生效类参数会被加载到内存。因此，不推荐使用该方式修改参数。

